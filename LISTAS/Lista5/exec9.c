#include <stdio.h>

int main() {
    int nota, continuar;
    int quantidade = 0; soma = 0;
    float media;
    do {
        
    } while(continuar != 0);
    
    if(quantidade > 0) {
      media = (float) soma / quantidade;
      printf("Media: %.2f\n", media);
    } else {
        printf("Nenhuma nota valida registrada.\n");    
    }

    return 0;
}